;; Poll for wallpapers (original paths for onclick)
(defpoll wallpaper_list :interval "5s"
  "bash ~/.config/eww/scripts/get_wallpapers_with_thumbnails.sh"
)

;; Poll for display paths (thumbnails for GIFs, originals for others)
(defpoll display_list :interval "5s"
  "bash ~/.config/eww/scripts/get_display_paths.sh"
)

(defvar fades "simple center wipe outer slide")
(defvar selected_fade "wipe")

(defwindow wallpaper_widget
  :monitor 0
  :geometry (geometry :x "0%" :y "0%" :width "100%" :height "100%")
  :stacking "overlay"
  :focusable true
  :exclusive false
  :namespace "wallpaper_widget"
  :wm-ignore false
  (eventbox :onclick "eww close wallpaper_widget"
            :class "overlay-background"
    (box :orientation "vertical" :halign "center" :valign "center"
      (eventbox :onclick "echo 'clicked inside'" ; Prevent event bubbling
        (box :class "wallpaper-widget-content"
             :orientation "vertical" 
             :spacing 10
          (wallpaper_buttons :wallpaper_list wallpaper_list :display_list display_list :selected_fade selected_fade)
        )
      )
    )
  )
)

(defwidget wallpaper_buttons [wallpaper_list display_list selected_fade]
  (scroll :vscroll true :hscroll false :height 330
    (box :orientation "vertical" :spacing 8
      ; First row - close button and wallpapers 0, 1
      (box :orientation "horizontal" :spacing 8 :halign "center"
        (button :class "close-button"
                :onclick "eww close wallpaper_widget"
                :tooltip "Close"
          "✕")
        (for i in "[0, 1]"
          (box :width 180 :height 110 :class "wallpaper-tile"
            (button
              :class "wallpaper-button"
              :onclick "bash ~/.config/eww/scripts/change_wallpaper.sh '${wallpaper_list[i]}' '${selected_fade}'"
              (image :path "${display_list[i]}" 
                     :image-width 160 
                     :image-height 90)))))
      ; Second row - wallpapers 2, 3, 4
      (box :orientation "horizontal" :spacing 8 :halign "center"
        (for i in "[2, 3, 4]"
          (box :width 180 :height 110 :class "wallpaper-tile"
            (button
              :class "wallpaper-button"
              :onclick "bash ~/.config/eww/scripts/change_wallpaper.sh '${wallpaper_list[i]}' '${selected_fade}'"
              (image :path "${display_list[i]}" 
                     :image-width 160 
                     :image-height 90)))))
      ; Third row - wallpapers 5, 6, 7
      (box :orientation "horizontal" :spacing 8 :halign "center"
        (for i in "[5, 6, 7]"
          (box :width 180 :height 110 :class "wallpaper-tile"
            (button
              :class "wallpaper-button"
              :onclick "bash ~/.config/eww/scripts/change_wallpaper.sh '${wallpaper_list[i]}' '${selected_fade}'"
              (image :path "${display_list[i]}" 
                     :image-width 160 
                     :image-height 90)))))
      ; Fourth row - wallpapers 8, 9, 10
      (box :orientation "horizontal" :spacing 8 :halign "center"
        (for i in "[8, 9, 10]"
          (box :width 180 :height 110 :class "wallpaper-tile"
            (button
              :class "wallpaper-button"
              :onclick "bash ~/.config/eww/scripts/change_wallpaper.sh '${wallpaper_list[i]}' '${selected_fade}'"
              (image :path "${display_list[i]}" 
                     :image-width 160 
                     :image-height 90)))))
      ; Fifth row - wallpapers 11, 12, 13
      (box :orientation "horizontal" :spacing 8 :halign "center"
        (for i in "[11, 12, 13]"
          (box :width 180 :height 110 :class "wallpaper-tile"
            (button
              :class "wallpaper-button"
              :onclick "bash ~/.config/eww/scripts/change_wallpaper.sh '${wallpaper_list[i]}' '${selected_fade}'"
              (image :path "${display_list[i]}" 
                     :image-width 160 
                     :image-height 90)))))
      ; Sixth row - wallpapers 14, 15, 16
      (box :orientation "horizontal" :spacing 8 :halign "center"
        (for i in "[14, 15, 16]"
          (box :width 180 :height 110 :class "wallpaper-tile"
            (button
              :class "wallpaper-button"
              :onclick "bash ~/.config/eww/scripts/change_wallpaper.sh '${wallpaper_list[i]}' '${selected_fade}'"
              (image :path "${display_list[i]}" 
                     :image-width 160 
                     :image-height 90)))))
    )
  )
)

